[build-system]
requires = [
    "setuptools",
    "wheel",
    "pip",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 79
extend-exclude = "/tests"
target-version = [
    "py36",
    "py37",
    "py38",
    "py39",
    "py310",
]
